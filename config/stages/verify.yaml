api_version: "1"
id: verify
version: 1
description: Independently verify the implementation before completion.
purpose: Confirm acceptance criteria, test coverage, and quality before merge.
preconditions:
  - an implementation diff exists with passing project tests
completion_evidence:
  - independent verification confirms every acceptance criterion
role:
  id: qa-engineer
  min_version: 1
model_alias: verification
capability_profile:
  id: read-only-verification
  min_version: 1
skills:
  - id: systematic-debugging
    version: 1.0.0
  - id: verification-before-completion
    version: 1.0.0
budgets:
  time_seconds: 1200
  tokens: 80000
  context_tokens: 80000
  cost_usd: 4.0
  retry_limit: 1
  attempt_limit: 2
decision_required: false
approval_required: true
fallback_stage: implement
resume_supported: true
knowledge_required: false
cleanup_gate: true
